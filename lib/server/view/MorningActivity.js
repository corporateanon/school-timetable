import React from 'react';
import {ordFemale} from '../i18n/ordinals';
import moment from 'moment';


const MorningActivity = ({activity, now}) => (
  <div className="activity-morning">
    <p>Уроки ще не почалися.</p>
  </div>
);

export default MorningActivity;
